<h1><%= work.title %></h1>
<img src="<%= work.imageFile %>" alt="<%= work.description %>">
<div><%= work.description %></div>
<div>By <%= work.artist.first_name %> <%= work.artist.last_name %>, <%= work.yearCreated %></div>
<% if (currentUser.usertype == 'gallery') { %>
    <input type="submit" value="Add to Collection">
<% } %> 