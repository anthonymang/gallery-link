<h1><%= artist.name %></h1>

<ul>
<% works.forEach(work =>{ %>
    <li><%=work.title %>, <%= work.createdDate %> </li>
    <img src="<%= work.imageFile %>" alt="<%= work.description %>">
    <div><%= work.description %></div>
<% }) %> 
</ul>